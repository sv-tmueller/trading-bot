# Local config for scripts/status.sh — the operator's read-only status check.
# Copy this file to .env.status (gitignored) and fill in the values.
# See docs/runbooks/status-check.md for the one-time setup.

STATUS_URL=https://<ref>.supabase.co/functions/v1/status
STATUS_TOKEN=
