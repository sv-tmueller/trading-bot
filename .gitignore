.env
storage/*.db
trading_bot.db
reports/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
.DS_Store
venv/
.venv/
logs/

# Supabase local
supabase/.temp/
supabase/.branches/

# Claude Code local overrides
.claude/settings.local.json
.env.local
.env*.local

# Local status-check config (#354) — STATUS_URL/STATUS_TOKEN; see .env.status.example
.env.status

# Local equity-backfill config (#389) — Alpaca + Supabase service-role
# creds for the one-time scripts/backfill_equity_snapshots.ts run; see
# .env.backfill.example
.env.backfill

# review-workflow output (generated by /tm-review-changes and /tm-review-codebase)
/reviews/

# Downloaded FXCM EUR/USD archive cache (#371) — never committed; re-fetch via
# backtest/run_fx_plumbing_check.py --fetch (see backtest/fx_data.py docstring).
/data/

# Exported live trading history (#403) — CSVs from scripts/export_live_history.sh,
# consumed by backtest/run_live_divergence.py. Account data; never committed.
/live_export/

# Local Alpaca paper-account API capture config (#479 T2) — ALPACA_API_KEY/
# ALPACA_SECRET_KEY for scripts/capture_alpaca_shapes.sh's four read-only
# GETs. Paper keys only; never committed.
.env.capture

# Local weekly-review config (#481) — Supabase service-role creds for
# scripts/render_weekly_journal.ts; see .env.weekly.example
.env.weekly
